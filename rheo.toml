version = "0.6.0"
content_dir = "content"
formats = ["html"]

# `template.typ` is a library, not a page — without this it would compile to
# its own `template.html`.
[spine]
exclude = ["template.typ"]

[html.assets]
css_stylesheet = "./style.css"
js_scripts = "./index.js"
